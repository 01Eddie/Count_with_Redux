/* import { CounterType, DecrementAction, IncrementAction } from "../../types/type";

export const incrementCounter = (): IncrementAction => ({
    type: CounterType.INCREMENT
});

export const decrementCounter = (): DecrementAction => ({
    type: CounterType.DECREMENT
});
 */
export const Borrador = "Borrado"